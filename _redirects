# Netlify redirects — Atelier MAUDÉ
# Format: /source  /destination  status_code

# Redirect old GitHub Pages URL to root (update when custom domain is live)
# https://ateliermaude.github.io/atelier-maude/*  /:splat  301
